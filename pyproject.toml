[project]
name = "ternfold"
version = "0.1.0"
description = "Supabase-backed Margin Decision Desk MVP"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.vercel]
entrypoint = "ternfold.app:app"
